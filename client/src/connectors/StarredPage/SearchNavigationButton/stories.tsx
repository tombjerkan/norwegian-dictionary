import React from "react";
import { storiesOf } from "@storybook/react";
import SearchNavigationButton from ".";

storiesOf("SearchNavigationButton", module).add("default", () => (
    <SearchNavigationButton />
));
