import React from "react";
import { storiesOf } from "@storybook/react";
import StarredNavigationButton from ".";

storiesOf("StarredNavigationButton", module).add("default", () => (
    <StarredNavigationButton />
));
